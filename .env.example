# Vercel Blob — legacy pull/publish (publish-shared-model.bat)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_

# VPS Postgres — publish-shared-model-postgres.bat (tunnel localhost:5433 → VPS:5432)
# DATABASE_URL=postgresql://earlyuser:PASSWORD@127.0.0.1:5433/earlydb
