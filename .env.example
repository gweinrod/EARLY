# VPS Postgres — pull/train scripts (SSH tunnel localhost:5433 → VPS:5432)
# DATABASE_URL=postgresql://earlyuser:PASSWORD@127.0.0.1:5433/earlydb

# VPS API (server/.env) — JWT for login
# JWT_SECRET=your-long-random-secret
