# Reference audio

Place short WAV clips here for offline feature extraction (`src/reference-offline.ts`).

1. Add files, e.g. `bat-short-vowel.wav`
2. List paths in `REFERENCE_AUDIO_PATHS` in `src/main.ts`
3. Open devtools console after load to see MFCC summaries

Used to build training benchmarks in later phases.
